on:
  push:
      branches:
      - Test

name: 🚀 Deploy website to test env on push
jobs:
  simply-deploy:
    name: 🎉 Deploy to Simply
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.2
      with:
        server: linux296.unoeuro.com
        username: madswp.dk
        password: ${{ secrets.ftp_password }}
        server-dir: /odc_website/laravel/
  oracle_cloud-deploy:
    name: 🎉 Deploy to Oracle cloud
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.2
      with:
        server: odc.madswp.dk
        username: Mads_Automated_deploy
        password: ${{ secrets.Oracle_FTP_key }}
        server-dir: /
