on:
  push:
      branches:
      - production

name: 🚀 Deploy website to production
jobs:
  deploy-legacy:
    name: 🎉 Deploy to legacy production
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.2
      with:
        server: linux168.unoeuro.com
        username: odensedansecent.dk
        password: ${{ secrets.ODC_FTP_PASSWORD }}
        server-dir: /laravel/

